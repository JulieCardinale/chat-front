/* Imports */
import ChatRouter from 'ChatRouter';

/* * * * * * * * * *
 * * Chat component *
 *
 * @description : The main component, it contains the router.
 *
 */
const Chat = () => (
  <div className="chatroom">
    <ChatRouter />
  </div>
);

/* Export component */
export default Chat;
